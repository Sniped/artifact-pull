// repo constants
// TODO: allow management of the artifacts of multiple repos

export const REPO_NAME = 'plugin-test';
export const REPO_OWNER = 'Sniped';

// time constants

export const MINUTE_SECONDS = 60;
export const HOUR_SECONDS = MINUTE_SECONDS * 60;